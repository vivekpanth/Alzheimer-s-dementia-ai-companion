# Session summariser tool — uses GPT-4o Mini to generate session summary text


async def summarise_session(session_log: list) -> str:
    """Generate a plain-English summary of the session log using GPT-4o Mini."""
    pass
