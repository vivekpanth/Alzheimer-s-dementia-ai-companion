# Concern flag tool — detects repeated distress patterns across sessions


async def flag_concerns(user_id: str, session_log: list) -> list:
    """Detect repeated distress topics across sessions and return concern flags."""
    pass
